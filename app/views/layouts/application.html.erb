<!DOCTYPE html>
<html class="h-100">
  <head>
    <title>BookmarkBuster</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column h-100">
    <!-- ナビゲーションバー -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to 'BookmarkBuster', root_path, class: 'navbar-brand' %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to dashboard_path, class: 'nav-link' do %>
                  <i class="bi bi-speedometer2"></i> ダッシュボード
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to bookmarks_path, class: 'nav-link' do %>
                  <i class="bi bi-bookmark"></i> ブックマーク
                <% end %>
              </li>
            <% end %>
          </ul>
          
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                  <i class="bi bi-person-circle"></i> <%= current_user.username.presence || current_user.email %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li>
                    <%= link_to profile_path, class: 'dropdown-item' do %>
                      <i class="bi bi-person"></i> プロフィール
                    <% end %>
                  </li>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <%= button_to destroy_user_session_path, method: :delete, class: "dropdown-item text-danger", data: { turbo_confirm: 'ログアウトしますか？' } do %>
                      <i class="bi bi-box-arrow-right"></i> ログアウト
                    <% end %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "nav-link" do %>
                  <i class="bi bi-box-arrow-in-right"></i> ログイン
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: "nav-link" do %>
                  <i class="bi bi-person-plus"></i> 新規登録
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- メインコンテンツ -->
    <main class="flex-shrink-0">
      <div class="container my-4">
        <% flash.each do |key, message| %>
          <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
            <%= message %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        
        <%= yield %>
      </div>
    </main>

    <!-- フッター -->
    <footer class="footer mt-auto py-3 bg-light">
      <div class="container">
        <div class="row">
          <div class="col-md-6">
            <h5 class="mb-3">BookmarkBuster</h5>
            <p class="text-muted">効率的なブックマーク管理ツール</p>
          </div>
          <div class="col-md-6">
            <h5 class="mb-3">リンク</h5>
            <ul class="list-unstyled">
              <%= link_to 'お問い合わせ', new_contact_path, class: 'text-decoration-none text-muted' %>
              <li><%= link_to '利用規約', terms_path, class: 'text-decoration-none text-muted' %></li>
              <li><%= link_to 'プライバシーポリシー', privacy_path, class: 'text-decoration-none text-muted' %></li>
            </ul>
          </div>
        </div>
        <hr>
        <div class="text-center text-muted">
          <small>&copy; <%= Time.current.year %> BookmarkBuster. All rights reserved.</small>
        </div>
      </div>
    </footer>
  </body>
</html>